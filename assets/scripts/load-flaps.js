$(document).ready(function(){
	var svgcontainer = $('svg');
	console.log(FlapConfiguration.components['Flap-1'].src);

// We need to load-in the first element from the configuration.js and append it to the SvG

	// svgcontainer.append()
});